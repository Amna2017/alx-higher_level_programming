#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if matrix == "--":
        return "--"
    elif matrix[[],[]] == matrix.clear():
        return ""
    else:
        for i in range(len(matrix)):
            matrix.append(matrix[i])
        return matrix
